import { NextResponse } from 'next/server'

// Notifications — Phase 2
export async function GET() {
  return NextResponse.json({ error: 'Notifications not implemented yet — Phase 2' }, { status: 501 })
}

export async function PATCH() {
  return NextResponse.json({ error: 'Notifications not implemented yet — Phase 2' }, { status: 501 })
}
